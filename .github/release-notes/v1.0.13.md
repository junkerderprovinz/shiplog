## Changed
- The Engine **Enable** setting is now an **on/off toggle** (a switch) instead of a
  Yes/No dropdown. Its On/Off label is shown in the **active Unraid language**
  (e.g. *Ein/Aus* in German), with an English fallback. The stored value
  (`ENABLE=true/false`) is unchanged, so existing settings keep working.
